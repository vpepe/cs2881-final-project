Instructions on how to use fingerprint_gepa.py:


1) 
`pip install -r requirements.txt`

2) 
`export OPENAI_API_KEY="sk-proj..._d5txBDzMA"`

(To check if it's set, use `echo $OPENAI_API_KEY`)

3)`export HF_TOKEN="hf_Zy...mkTLw"`

